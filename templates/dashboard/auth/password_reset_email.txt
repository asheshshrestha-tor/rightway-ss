Hello {{ user.get_short_name|default:user.get_username }},

Someone asked to reset the password for your Rightway Support Services
dashboard account ({{ user.get_username }}).

To choose a new password, open this link:

{{ protocol }}://{{ domain }}{% url 'dashboard:password_reset_confirm' uidb64=uid token=token %}

The link can be used once and expires in {{ timeout_days }} days.

If you did not request this, you can ignore this email - your password will
not change.

Rightway Support Services
